<div class="card mx-auto my-4 shadow" style="width: 30rem;">
<img class="card-img" src= "<%= @product.url%>" alt="Card image cap">
<div class="card-body">
    <h5 class="card-title"> <%= @product.name%> </h5>
    <p class="card-text"><%= @product.description%></p>
  </div>
  <ul class="list-group list-group-flush">
    <li class="list-group-item">Prix : <%= @product.price%>€</li>
    
    <li class="list-group-item">Categorie : <%= @product.category.name%></li>
  </ul>
  
    <div class="card-body">
      
      <a   <%= link_to "Retourner au catalogue", products_path,  :class => "btn btn-primary" %></a>
      <% if user_signed_in? %>
					<h6 class="btn btn-primary" href="#">Ajouter au panier</h6>
			<%end%>
    </div>
  
</div>
